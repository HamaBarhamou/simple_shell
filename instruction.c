#include "main.h"

/**
 * list_instructions - a function ...
 * @head: the chaine
 * @str: the chaine
 *
 * Return: 1 or 0
 */

instruction_l **list_instrctions(instruction_l **head, char *str)
{
	UNUSED(head);
	UNUSED(str);
	return (NULL);
}
