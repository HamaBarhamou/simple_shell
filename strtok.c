#include "main.h"

/**
 * _strtok - a function ...
 * @str: the chaine
 * @delim: the chaine
 *
 * Return: 1 or 0
 */

char	*_strtok(char *str, const char *delim)
{
	/* implement your code, see man strtok*/

	UNUSED(str);
	UNUSED(delim);
	return (NULL);
}
